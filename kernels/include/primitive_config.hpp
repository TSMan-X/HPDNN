#include "platform.hpp"

/* Primitive config (member of primitive class)
 * Base primitive class, if needed, inheriate this class and expand func and data member
 * Need to finish function as follows:
 * 1. Grid size
 * 2. Block Size
 * 3. Rate between L1 cache and Shared Memory
 * 4. wheter disable L1 cache
 * 5. other optimization config
 */
class primitiveConfig {
};
