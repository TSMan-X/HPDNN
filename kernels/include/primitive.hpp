#include "primitive_desc.hpp"
#include "primitive_config.hpp"

namespace prim {
/* Primitive Class (TODO)
 * 1) base class, integrate all classes as member(init/set/get):
 * primitive desc, param config, primitive config, stream, memory, primitive factory
 * 2) select best fit impl of operator
 * 3) execute primitive
 * */
class primitive {
};
}
